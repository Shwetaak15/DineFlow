{
  "name": "dineflow",
  "version": "1.0.0",
  "description": "Smart Hostel Dining Management System - Orchestration Workspace",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "db": "$HOME/.local/dineflow-mongo/bin/mongod --dbpath $HOME/.local/dineflow-mongo/data --port 27017 --bind_ip 127.0.0.1 --fork --logpath $HOME/.local/dineflow-mongo/mongod.log",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npm run build:frontend && npm start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
