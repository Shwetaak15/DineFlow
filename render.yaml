services:
  - type: web
    name: dineflow-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5050
      - key: MONGO_URI
        sync: false
      - key: JWT_SECRET
        sync: false
